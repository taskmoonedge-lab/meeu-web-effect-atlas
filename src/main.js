import { atlas } from './data.js';
const app=document.querySelector('#app');let selected=atlas.families[0]?.id;let q='';
function filtered(){return atlas.families.filter(f=>!q||JSON.stringify(f).toLowerCase().includes(q.toLowerCase()))}
function render(){const a=filtered(),f=atlas.families.find(x=>x.id===selected)||a[0];if(f)selected=f.id;app.innerHTML=`<header><div><b>MEEU / WEB EFFECT ATLAS</b><small>v0.7 · ${atlas.familyCount} families · ${atlas.variantCount} variants</small></div><input id=q placeholder="Search families, variants, tech..." value="${q}"></header><main><aside>${a.map(x=>`<button data-id="${x.id}" class="${x.id===selected?'on':''}"><b>${x.nameJa}</b><small>${x.category} / ${x.subcategory} · ${x.variants.length} variant${x.variants.length>1?'s':''}</small></button>`).join('')}</aside><section><div class=stage><div class=orb></div><div class=ring></div><h1>${f?.nameJa||''}</h1></div></section><aside class=detail><h2>${f?.nameJa||''}</h2><p>${f?.nameEn||''}</p><p>${f?.description||''}</p><h3>VARIANTS · ${f?.variants.length||0}</h3>${(f?.variants||[]).map(v=>`<div class=variant>${v.nameJa}</div>`).join('')}<h3>TECH</h3><p>${[].concat(f?.tech||[]).join(' / ')}</p></aside></main>`;document.querySelector('#q').oninput=e=>{q=e.target.value;render()};document.querySelector('main aside').onclick=e=>{let b=e.target.closest('[data-id]');if(b){selected=b.dataset.id;render()}}}
render();